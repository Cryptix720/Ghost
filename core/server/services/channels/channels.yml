index:
  template:
    - home
    - index

tag:
  filter: tags:'%s'+tags.visibility:public
  data:
    tag: %s
  slugTemplate: true,
  editRedirect: '#/settings/tags/:slug/'

author:
  filter: author:'%s'
  data: author/%s
  slugTemplate: true
  editRedirect: '#/team/:slug/'

